name: Test Workflow

on:
  workflow_dispatch:

jobs:
    stage:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v5
    
            - name: Run tests
              run: |
                echo "pwd"
                pwd
                echo "ls -al"
                ls -al
                echo "env"
                env
